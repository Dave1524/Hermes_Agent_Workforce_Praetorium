# Every claude.ai connector this account exposes to a Claude Code session, as the deny-list
# names it. MEASURED 2026-09-18 from `claude mcp list` run in ~ (rows prefixed `claude.ai `;
# the tool prefix is mcp__claude_ai_ + the name with spaces as underscores).
#
# tests/test_fleet_capabilities.sh::connector-deny-complete joins this list against
# buzz-team/agent-settings.json in BOTH directions: a connector added to the account and not
# here is invisible to the gate, so re-measure when one is added or removed and overwrite the
# marker above — never add a second one beside it.
mcp__claude_ai_Claude_Docs
mcp__claude_ai_Eden
mcp__claude_ai_Figma
mcp__claude_ai_Gmail
mcp__claude_ai_Google_Drive
mcp__claude_ai_Microsoft_365
mcp__claude_ai_Notion
