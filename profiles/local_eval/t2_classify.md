Classify the state of each systemd unit listed below.

For each unit output exactly one line:
<unit-name> = <verdict>

<verdict> must be one of: active, inactive, failed

Output one line per unit and nothing else. No prose, no headers, no fences.

UNITS AND THEIR RAW STATUS OUTPUT:
{{INPUT}}
