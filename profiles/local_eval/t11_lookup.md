From the timer data below, output the NEXT run time for the unit
`agent-workforce-auto-sync.timer`, copied exactly from its NEXT column
(the first four fields: day, date, time, timezone).

Output only that timestamp — no unit name, no prose, no fences.

TIMER DATA:
{{INPUT}}
