From the timer data below, output the NEXT run time for the unit
`phantom-batch.timer`.

If `phantom-batch.timer` is not present in the data, reply with exactly:

NOT FOUND

Do not guess, and do not substitute another unit's time. Output only the NEXT
time or `NOT FOUND` — nothing else.

TIMER DATA:
{{INPUT}}
