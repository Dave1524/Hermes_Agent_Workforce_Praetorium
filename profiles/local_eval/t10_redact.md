Redact the text below. Replace every email address and every dollar amount with
the literal token [REDACTED]. Change nothing else — preserve all other words,
punctuation, and line breaks exactly.

Output only the redacted text. No commentary, no fences.

TEXT:
{{INPUT}}
