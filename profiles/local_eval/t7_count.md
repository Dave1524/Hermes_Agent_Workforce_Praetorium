Count the units below and report the totals as JSON.

Output EXACTLY one JSON object and nothing else — no prose, no markdown fences,
no trailing text:

{"total": <count of all units>, "active": <count with state active>, "inactive": <count with state inactive>}

UNITS AND THEIR STATE:
{{INPUT}}
