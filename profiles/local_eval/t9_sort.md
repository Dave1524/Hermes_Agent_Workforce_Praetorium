Sort the unit names below into a single alphabetical list.

- Remove duplicates — each name must appear at most once.
- Case-insensitive alphabetical order.
- Output one name per line, and nothing else. No numbering, no prose, no fences.

NAMES:
{{INPUT}}
