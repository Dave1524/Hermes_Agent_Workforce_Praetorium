From the unit status data below, output the name of every unit whose state is
exactly `active`.

Output one unit name per line, and nothing else. No prose, no headers, no fences,
no state word — just the active unit names, exactly as written. Do not include
any unit whose state is not `active`.

UNITS AND THEIR STATE:
{{INPUT}}
