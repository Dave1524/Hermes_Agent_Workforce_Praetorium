#!/bin/sh
# Rendered by bin/fleet_capabilities.py from design/agents/marcus.toml — edit the manifest, not this file.
# buzz-agent@marcus names this shim with --mcp-command; the server name is this file's stem.
exec "$(dirname "$0")/buzz-team-mcp.py" --agent marcus --tools qmd,notion,brave "$@"
