[Service]
# Offload qmd's embedding/rerank/expansion GGUF models to the Intel Arc iGPU via Vulkan.
# Requires dave in the 'render' group (renderD128 access) — added 2026-07-12.
Environment=QMD_LLAMA_GPU=vulkan
SupplementaryGroups=render video
