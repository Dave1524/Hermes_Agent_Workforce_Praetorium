---
name: spec-to-code-enforcement
description: Validates code against markdown specs. Detects missing implementations, over-implementations, and constraint violations. Use for spec compliance audits, reviewing generated code, or auditing changes after refactors.
---

Canonical source: `08_skills/spec-to-code-enforcement/SKILL.md` in the vault; its bundled files sit beside it in
`08_skills/spec-to-code-enforcement/`. On this box: `~/vault/08_skills/spec-to-code-enforcement/`

This file is a pointer, never a copy. Read the canonical SKILL.md first and resolve its
relative references from that directory. The description above is the vault's, mirrored by
bin/pointer_skills_sync.py so the trigger text an agent reads is the one the vault wrote.
