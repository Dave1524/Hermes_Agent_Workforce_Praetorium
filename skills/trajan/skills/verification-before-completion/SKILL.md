---
name: verification-before-completion
description: Use when about to claim work is complete, fixed, passing, or ready. Use before committing, creating PRs, or declaring success. Activates when tempted to say "should work" or "tests pass" without fresh evidence.
---

Canonical source: `08_skills/verification-before-completion/SKILL.md` in the vault; its bundled files sit beside it in
`08_skills/verification-before-completion/`. On this box: `~/vault/08_skills/verification-before-completion/`

This file is a pointer, never a copy. Read the canonical SKILL.md first and resolve its
relative references from that directory. The description above is the vault's, mirrored by
bin/pointer_skills_sync.py so the trigger text an agent reads is the one the vault wrote.
