---
name: linkedin-content-engine
description: Generates high-conviction LinkedIn posts in Dave Hamelink's founder voice for Vantage Point Consulting (logistics / warehousing / cold chain) and writes them to the Notion LinkedIn Content Planner database. Use when Dave says "draft a LinkedIn post", "LinkedIn content about X", "carousel about X", "save this post to Notion", "/linkedin", "/linkedin-post", "linkedin post on [topic/observation]", "10 X about Y", "value-dense image post", "habits post", "list post", or asks for variations of an angle to test on LinkedIn. Each output is a save/share/DM-worthy post + Figma-ready carousel/infographic content object + hashtags + first-comment strategy, written to the Content DB at status "Draft" — never auto-published.
---

Canonical source: `08_skills/linkedin-content-engine/SKILL.md` in the vault; its bundled files sit beside it in
`08_skills/linkedin-content-engine/`. On this box: `~/vault/08_skills/linkedin-content-engine/`

This file is a pointer, never a copy. Read the canonical SKILL.md first and resolve its
relative references from that directory. The description above is the vault's, mirrored by
bin/pointer_skills_sync.py so the trigger text an agent reads is the one the vault wrote.
