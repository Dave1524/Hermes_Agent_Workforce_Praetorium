---
name: post-call-capture
description: "VP Sales Skill 3: Post-Conversation Capture Autopilot. After any client or prospect call, Dave does a 30-second brain dump and Claude handles all follow-up system updates: (1) update/create Notion Client Pipeline entry, (2) extract reusable insights to vault 05_knowledge/, (3) create follow-up tasks with due dates in Notion Task Inbox, (4) add new open loop to vault if a decision emerged, (5) append to today's vault daily log. Closes the loop so no deal dies from missing follow-through. Use when Dave says 'post-call [company]', 'capture call with [person]', 'log call [company]', '/post-call [company]', or 'debrief [company]'."
---

Canonical source: `08_skills/post-call-capture/SKILL.md` in the vault; its bundled files sit beside it in
`08_skills/post-call-capture/`. On this box: `~/vault/08_skills/post-call-capture/`

This file is a pointer, never a copy. Read the canonical SKILL.md first and resolve its
relative references from that directory. The description above is the vault's, mirrored by
bin/pointer_skills_sync.py so the trigger text an agent reads is the one the vault wrote.
